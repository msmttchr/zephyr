sample:
  name: RCP BLE / OT with UART multiplexer
  description: >
    Radio Co-Processor application exposing multiple virtual UART
    channels over a single asynchronous UART using a proprietary
    multiplexing protocol.

  tags:
    - uart
    - multiprotocol
    - rcp

  depends_on:
    - uart

  platforms:
    allow:
      - nucleo_wba65ri

tests:
  sample.rcp_ble_ot.build:
    build_only: true
    tags:
      - uart
      - rcp

